# Use postgres/example user/password credentials
version: '3.1'
services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: tts
      POSTGRES_PASSWORD: taipei
      POSTGRES_DB: taipei
    ports:
      - 5432:5432


